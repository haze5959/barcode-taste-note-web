{
  "name": "barnote-og-worker",
  "private": true,
  "version": "0.1.0",
  "description": "BarNote 소셜 크롤러용 OG 메타태그 주입 Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.20.0"
  }
}
